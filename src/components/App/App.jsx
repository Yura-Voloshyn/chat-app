import { ChatWrapper } from '../ChatWrapper/ChatWrapper';
import { StyledContainer } from './App.styled';
export const App = () => {
  return (
    <StyledContainer>
      <ChatWrapper />
    </StyledContainer>
  );
};
